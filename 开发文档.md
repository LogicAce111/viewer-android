# Viewer Android 1.2.0 开发文档

## 1. 项目定位

Viewer Android 是目录驱动、完全本地的四分类媒体应用。产品边界是“读取用户明确授权的目录并播放内容”，不承担文件管理、全盘扫描、联网元数据或云服务职责。

工程目录：`E:\Viewer\Android`  
应用 ID / namespace：`com.legion.viewer`  
最终版本：`1.2.0`（versionCode 4）。全部优化均已通过用户实机验收，沿用原应用ID和正式签名打包，跳过1.1。版本变化见 `CHANGELOG.md`。

## 2. 技术栈

- Kotlin 2.3.21
- Android Gradle Plugin 9.2.0
- Gradle Wrapper 9.4.1
- Java/Kotlin 字节码目标17，构建使用JDK 21
- Jetpack Compose + Material 3（Compose BOM 2026.06.01）
- Coroutines / Flow
- DataStore 1.2.1
- Room 2.8.4
- LibVLC 3.7.5
- Coil 3.5.0
- CommonMark 0.27.1
- minSdk 26，compile/target SDK 37.1
- 仅构建 `arm64-v8a`

## 3. 工程结构

```text
Android/
├─ app/src/main/java/com/legion/viewer/
│  ├─ data/       SAF扫描、持久目录索引、DataStore、Room、缩略图和日志
│  ├─ model/      分类、媒体、设置、扫描与播放状态模型
│  ├─ playback/   LibVLC媒体源、控制器、后台服务与MediaSession
│  └─ ui/         Compose页面、主题、自定义图标和通用控件
├─ app/src/main/res/   清单、主题、自适应图标和字符串资源
├─ scripts/            Debug与Release构建脚本
├─ artifacts/          本机构建产物，不进入Git
├─ 使用说明.md
├─ 开发文档.md
└─ THIRD-PARTY-NOTICES.md
```

应用使用单一 `app` 模块和手动依赖容器 `AppContainer`，不引入 Hilt。`ViewerApplication` 创建长期存活的仓库、扫描器和播放控制器，`ViewerViewModel` 维护页面导航与扫描状态。

## 4. 核心模型与数据流

- `MediaCategory`：Video、Music、Text、Comics，并定义各分类扩展名白名单。
- `CategorySource`：分类对应的 SAF Tree URI、显示名和启用状态。
- `MediaItem`：Content URI、Document ID、相对路径、分类、分组和修改时间等。
- `ComicWork`：漫画目录、封面和页面集合。
- `PlaybackOrder`：RepeatOne、Sequential、Shuffle。
- `PlaybackSnapshot`：当前媒体、队列、播放状态、进度、时长、能力和错误信息。

UI 通过 `StateFlow` 收集设置、扫描和播放快照。页面只发出用户意图，持久化与 LibVLC 生命周期由仓库和控制器处理。

## 5. Storage Access Framework 与扫描

四个分类分别通过 `OpenDocumentTree` 获取目录。授权后调用 `takePersistableUriPermission`，Tree URI 与目录名写入 DataStore。

`DocumentMediaScanner` 使用 `DocumentsContract` 迭代遍历：

1. 从授权根 Document ID 开始。
2. 读取子文档，跳过隐藏或不可读项。
3. 目录进入待扫描队列，文件按当前分类扩展名过滤。
4. 保存相对目录层级并使用自然排序。
5. 根目录查询或遍历失败返回错误；子目录失败增加跳过计数，继续读取其他目录。

`PersistentIndexLoader` 首先读取应用私有目录 `files/media-index` 中对应分类的快照。快照绑定分类和完整 Tree URI，保存有序文件元数据、忽略数和跳过数；空列表同样是有效结果。只有缺少索引（包括旧版本首次升级）、所选目录改变或用户明确刷新时才调用扫描器。不会因切回页面、应用进程重启或外部文件变化自动遍历目录。索引损坏或版本不支持时显示手动刷新入口。

`FileMediaIndexStore` 使用带格式版本和SHA-256校验的二进制文件，在IO线程读写。写入同目录临时文件并同步后原子替换，取消或失败不覆盖上一次完整索引。未完整读取全部子目录时不提交新快照，保留旧列表；没有旧列表则暂时显示本次可读部分并提示刷新。磁盘写入失败仍可浏览本次结果，但明确提示尚未保存。

`IndexOperationQueue` 按分类串行执行加载、刷新、换目录和清除。新请求取消前一个请求，并等待旧请求释放互斥锁，避免不可取消的SAF查询或文件写入与新目录操作交错。更换或清除目录先删除该分类的索引，再更新DataStore。界面等待DataStore真实设置后初始化，避免把启动占位设置误认为未配置。错误或刷新状态保留可用列表，仍可打开内容；失败后切换页面不自动重试。

## 6. 音视频播放

`PlaybackController` 是唯一 LibVLC 会话所有者，负责打开、切换、保存进度、播放顺序和播放器事件。`SafMediaSource` 通过 `ContentResolver.openAssetFileDescriptor` 打开媒体，失败时回退 `openFileDescriptor`，并在媒体会话结束后关闭 descriptor。

主要规则：

- 打开请求带版本号，快速切换时只有最后请求生效。
- 打开超过12秒且未进入有效状态时报告失败。
- 视频优先硬件解码，失败后最多进行一次兼容解码重试。
- 音频由 `PlaybackService` 提供前台服务、MediaSession、通知、音频焦点和耳机事件。
- 音乐允许后台继续；视频进入后台时暂停并保存进度。
- 音视频进度周期保存，并在暂停、切换和离开时补充保存。

视频 UI 使用 `VLCVideoLayout` 填满播放器区域，标题和控制层透明叠加，不参与视频尺寸测量。单击显隐、双击播放暂停，播放3秒无操作后自动隐藏。

历史恢复使用 `PlaybackResumeAttempt`：有效非零历史位置提交至VLC后，等待对应打开请求的原生时间事件确认。确认窗口为5秒，时间容差1秒；手动跳转、终止失败及已确认的解码重试不再次产生提示。事件绑定打开请求和播放器代次，过期来源事件丢弃。`PlaybackSnapshot.resumeNotice`携带请求ID及单调时钟过期时间，UI消费一次并在控制栏上方显示3秒；不作为播放错误或加载状态消息复用。

## 7. 文本与漫画阅读

`ReaderChrome.kt` 共用阅读页显隐、3秒空闲计时及生命周期处理。上下栏在固定正文视口上叠加，正文不应用系统栏或缺口边距；只有工具栏避让缺口和系统栏，避免固定纯色留白。前台恢复时显示控制层，退出时恢复系统栏及先前的系统栏行为。系统栏允许边缘滑动临时显示。

Compose 在 Initial 事件阶段观察交互，不消费正文滚动和控件事件；正文任意位置点击在子节点处理后确认。Markdown 使用 `ReaderWebView.dispatchTouchEvent` 观察原生触摸并完整交给 WebView。两者共用 `ReaderTapGesture` 排除长按、多指、越出页面和超过触摸阈值的拖动。按钮、滑块、滚动条不会触发显隐。滚动变化继续重置计时，持续触摸期间暂停倒计时。

`MainActivity` 在 Android 11及以上明确允许窗口进入全部缺口区域，Android 9/10使用短边模式；保留 `enableEdgeToEdge` 并禁用系统状态栏对比度底色。该窗口配置同时用于视频，视频安全边距只作用于叠加控制层，画面仍使用完整窗口。

TXT 完成进度恢复、Markdown 页面加载完成、漫画首次定位的可见图片加载成功后才允许自动隐藏。首次加载或当前图片错误时显示控制层；正常滚动中的后续图片加载不会唤出已隐藏的工具栏。显隐不重建正文、WebView 或列表状态。

`ReaderTools.kt`负责阅读恢复提示、返回顶部按钮和3秒撤销窗口。提示在实际历史定位完成后触发一次，计时基于单调时钟，不随交互重置；撤销窗口阻止自动隐藏，但允许正文点击手动隐藏。顶部操作完成后才提供撤销，过期时释放位置快照。

TXT与漫画记录列表项、项内像素偏移及比例；布局不变时按精确位置撤销，字号/宽度/视口变化时按比例恢复。漫画缓存已加载页的宽高比，避免滚回卸载页时先按默认图片比例布局。Markdown记录像素位置、比例及布局信息，重新排版后使用比例恢复。正文布局不因提示显隐而变化。

进度恢复完成前不写文本进度；Markdown重排时保留当前位置，零进度也正常恢复。顶部操作、撤销及离开页面保存最终位置。漫画离开/后台保存通过ViewModel作用域执行，避免阅读页销毁取消保存任务。恢复提示与阅读导航无需数据库迁移。

TXT 解码顺序：BOM、严格 UTF-8、GB18030 回退。TXT 被切成约4KB的字符范围并交给 `LazyColumn` 惰性排版，不复制整本小说，也不让 WebView 一次性布局大型文本。

Markdown 由 CommonMark 转换为 HTML，并在受限 WebView 中显示。WebView 禁用 JavaScript、网络、文件访问、Content访问、DOM存储和外部导航。

文本阅读比例保存在 Room：滚动停止后防抖保存，离开页面和生命周期 `ON_STOP` 时再次保存。右侧拖动条可以直接改变滚动比例。

漫画按目录分组，Coil 从 Content URI 加载图片。连续阅读使用惰性列表并预取可见范围附近页面；当前页按视口中心计算，页码和页内偏移保存到 Room。

漫画库通过 `ComicDirectories.kt` 将图片的实际相对路径映射为作品：直接包含图片的目录是一部漫画，该目录的父目录是分类。使用实际路径而非显示用的“根目录”标签作为键，防止真实同名文件夹与根目录散图合并；根路径内部为空字符串。每组按自然顺序显示作品，组名使用完整父路径，根分组优先。封面URI与页面顺序保持原有语义，已有阅读进度仍按封面URI恢复。

`ComicGrid` 复用其他分类的 `DirectoryGroupHeader`，默认展开，标题统计作品数；折叠只隐藏封面，不改变作品集合。网格保持原封面样式并按60本逐批显示，加载数量只计算已展开的作品。该阶段不改变扫描器或数据库。

## 8. 持久化

DataStore 保存：四分类目录URI和名称、应用主题、音量、静音、倍速、播放顺序、文本外观和漫画宽度。

Room 数据库 `viewer.db` 保存各 Content URI 的音视频时间点、文本滚动比例、漫画页码和页内偏移。扫描结果和媒体正文不写入数据库。

目录索引独立保存在 `files/media-index/{Video,Music,Text,Comics}.index`，不使用可被系统清理的缓存目录。索引只包含元数据，不复制视频、音乐、文本或漫画正文；实际打开内容仍通过原来的Content URI读取。索引持久化不修改Room结构或原有进度记录。

## 9. 权限与安全

Manifest 仅声明 `FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_MEDIA_PLAYBACK` 和 `POST_NOTIFICATIONS`。应用不声明网络权限或全盘存储权限。Markdown WebView 禁止脚本与网络。日志位于应用私有目录，轮转保留5个文件、每个最多2MB，不记录媒体正文。

## 10. 本地构建

准备 Android SDK 后在 `local.properties` 配置：

```properties
sdk.dir=D\:\\Android\\Sdk
```

Debug 构建：

```powershell
cd E:\Viewer\Android
.\scripts\Build-Debug.ps1
```

输出：`artifacts\apk\Viewer-Android-arm64-debug.apk`。

## 11. 正式签名与发布

签名密钥必须保存在项目之外。当前约定位置：

```text
E:\Viewer\signing\viewer-android-release.jks
E:\Viewer\signing\viewer-android-signing.properties
```

签名属性格式：

```properties
storeFile=E:/Viewer/signing/viewer-android-release.jks
storePassword=请填写密钥库密码
keyAlias=viewer
keyPassword=请填写密钥密码
```

构建正式版：

```powershell
.\scripts\Build-Release.ps1
```

脚本执行 Release Lint、现有 Debug 单元测试任务和签名 APK 构建，并输出：

```text
artifacts\release\1.2.0\Viewer-Android-arm64-v1.2.0.apk
artifacts\release\1.2.0\SHA256SUMS.txt
```

版本号从 `app/build.gradle.kts` 读取，避免覆盖旧版本产物。同时复制使用说明、开发文档、更新说明和第三方声明。发布前使用 Android SDK `apksigner verify --verbose --print-certs` 检查签名。

必须长期备份 `.jks` 和对应密码。丢失密钥后无法为已安装的正式版提供可覆盖升级的 APK。不要把密钥、签名属性或 `local.properties` 提交到 Git。

## 12. Git准备

项目使用Git，远程仓库为 `https://github.com/LogicAce111/viewer-android`，主分支为 `main`。现有 `.gitignore` 已排除 `.gradle`、`.idea`、`local.properties`、构建目录、artifacts、抓图以及项目内的常见密钥文件。源码与自动化测试提交到仓库，正式APK及校验文件上传至GitHub Releases；签名文件保留在项目外。

## 13. 当前边界

1.0.0 不包含字幕、歌词、联网元数据、搜索、收藏、文件管理、账号、云同步、EPUB、CBZ、PDF、Office、画中画、Android TV、Chromecast、Android Auto、AAB和非ARM64 ABI。
