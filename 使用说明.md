# Viewer Android 1.0.0 使用说明

## 1. 应用简介

Viewer 是一款完全离线的本地媒体中心，将视频、音乐、小说文本和漫画图片集中到同一个应用中。它不会扫描整部手机，也不会移动、重命名或删除文件；只有用户通过系统目录选择器授权的目录才会被读取。

四个分类可以选择同一个目录，也可以选择互相嵌套的目录。每个分类只显示自己支持的格式，因此混合存放的文件不会串到其他分类。

## 2. 系统要求与安装

- Android 8.0 或更高版本。
- 仅支持 ARM64（`arm64-v8a`）手机和平板。
- 安装 APK 时，系统可能要求允许当前文件管理器“安装未知应用”。
- 覆盖升级必须使用相同的 Viewer 正式签名；若安装过开发调试版，Android 可能要求先卸载调试版。卸载会清除应用设置和阅读进度，请先确认后操作。

Viewer 不申请全盘存储权限，也不申请网络权限。Android 13 及以上可选择授予通知权限，以使用音乐播放通知和锁屏控制；拒绝后应用内播放仍可使用。

## 3. 首次配置目录

1. 打开 Viewer，在主页点击视频、音乐、文本或漫画卡片。
2. 点击“选择目录”，在 Android 系统目录选择器中找到媒体所在文件夹。
3. 点击“使用此文件夹”并确认授权。
4. 其余分类可按同样方式配置，也可以暂时留空。

目录也可在右上角“设置”页面中更换或清除。重新选择目录后会重新扫描；平时进入分类不会自动刷新，外部增删文件后请点击分类页右上角的刷新按钮。

Android 11 及以上系统不允许选择存储根目录、Download 根目录、`Android/data` 或 `Android/obb`，这是系统目录选择器的限制。

## 4. 支持格式

| 分类 | 支持格式 |
| --- | --- |
| 视频 | MP4、MKV、AVI、MOV、WMV、WebM、M4V |
| 音乐 | MP3、FLAC、WAV、AAC、M4A、OGG、Opus、WMA |
| 文本 | TXT、Markdown（MD）；支持 UTF-8、UTF-16，无法按 UTF-8 解码时回退 GB18030 |
| 漫画 | JPG/JPEG、PNG、WebP、GIF、BMP |

格式在列表中出现不代表设备一定具备对应编码器。Viewer 使用 LibVLC 播放音视频，遇到设备硬件解码不兼容时会尝试兼容解码；损坏文件或特殊编码仍可能无法播放。

## 5. 分类浏览

- 扫描会递归读取已授权目录的子目录，并按数字友好的自然顺序排列，例如“第2集”排在“第10集”之前。
- 目录标题可以折叠或展开，标题旁显示该目录的文件数量。
- 视频以双列封面卡片显示，封面右下角显示可读取到的视频时长。
- 音乐和文本以紧凑列表显示。
- 漫画按目录组成作品；同一子目录的图片是一部漫画，根目录图片组成“根目录”作品。
- 不属于当前分类的文件会被忽略，并在页面顶部显示忽略数量。

## 6. 视频播放

- 单击视频画面：显示或隐藏标题栏和控制栏。
- 双击视频画面：播放或暂停。
- 播放期间无操作3秒后控制层自动隐藏；暂停、拖动进度、加载、出错或打开高级设置时保持显示。
- 底部控制依次为播放顺序、上一项、后退5秒、播放/暂停、前进5秒、下一项和全屏。
- 点击标题栏右侧“调节”按钮，可设置静音、音量和倍速；再次点击或按手机返回键可关闭。
- 返回键优先关闭高级设置，其次退出全屏，最后返回视频列表。

播放顺序包括单曲循环、顺序播放和随机播放。顺序播放到最后一项后回到第一项；随机播放在多项时避免立即重复当前文件。

## 7. 音乐播放

- 音乐播放页中部保留当前播放队列，可滚动并直接点击其他歌曲切换。
- 底部提供播放顺序、上一项、前后5秒、播放/暂停和下一项。
- 标题栏右侧“调节”按钮用于静音、音量和倍速。
- 离开播放页、锁屏或切到其他应用后，音乐可继续播放。
- 通知栏和锁屏提供播放控制；耳机拔出或失去音频焦点时会按系统规则暂停。
- 应用其他页面底部可能显示迷你播放器，点击可返回完整播放页。

## 8. 文本阅读

- TXT 使用分块惰性排版，大型小说不会一次性加载整个页面。
- Markdown 使用禁用脚本、网络、文件访问和外部导航的受限 WebView 渲染。
- 底部阅读设置可调整字号、行距和内容宽度；右上角可切换阅读背景明暗。
- 右侧阅读定位条可直接点击或拖动。触摸后滑块会加粗，停止操作3秒后恢复细线。
- 阅读位置会在滚动停止、离开页面和应用进入后台时保存；下次打开同一文件自动恢复。

## 9. 漫画阅读

- 点击作品卡后进入纵向连续阅读。
- 图片按自然顺序排列，只加载当前页面附近的图片。
- 标题栏显示当前页/总页数。
- 底部可调整漫画显示宽度，阅读页支持隐藏或显示设置栏。
- 当前图片和页内位置会自动保存并在下次打开时恢复。

## 10. 主题、数据与故障处理

- 在主页或设置入口切换深色/浅色主题，选择会自动保存。
- 目录授权、播放设置、阅读参数和各媒体进度保存在应用私有数据中。
- 更换文件路径或撤销系统目录授权后，原进度记录可能无法对应新 URI。
- 若列表没有更新，请点击刷新；若目录失效，请重新选择目录。
- 若媒体长时间无法打开，Viewer 会结束加载并显示重试或返回，不会阻塞整个应用。
- 卸载应用会清除目录配置和进度数据库；重新安装不会自动恢复这些数据。

## 11. 1.0.0 功能边界

Viewer 不支持字幕、歌词、专辑或影片联网信息、搜索、收藏、文件管理、云同步、EPUB、CBZ、PDF、Office 文档、画中画、Android TV、Chromecast 或 Android Auto。
