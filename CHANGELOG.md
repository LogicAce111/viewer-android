# 更新说明

## 1.3.0

### 新增搜索

- 四大模块右上角新增搜索入口，位于刷新按钮左侧，支持按名称和目录搜索当前模块的全部已导入内容。
- 支持中文、英文大小写不敏感匹配及空格分隔的多关键词搜索；停止输入约200毫秒后更新结果，无需重新扫描目录，不读取文本正文。
- 漫画按作品名和所在目录搜索，输入合集名可查找其中的整本漫画，不会列出单张图片。
- 搜索结果保留目录分组和自然排序，显示匹配数量；阅读、播放或设置返回后保留关键词与结果位置，关闭或清空搜索恢复普通列表位置。
- 从视频、音乐搜索结果开始播放时，以全部匹配结果作为播放队列；后续修改或关闭搜索不影响正在播放的队列。
- 搜索框采用紧凑高度设计，常规字号下约48dp，保留原左右宽度、圆角和清空按钮。

### 浏览与导航修复

- 修复播放或阅读后返回模块列表跳到顶部的问题，四模块分别保留滚动位置、目录展开/折叠状态及已加载数量。
- 浏览状态在本次应用使用期间保留，切换模块和横竖屏后仍可恢复；完全退出后重新打开从顶部开始。
- 刷新后优先恢复原可见项目，项目已删除时回退到邻近位置；更换或清除目录重置对应模块状态。
- 设置页返回实际打开它的主页或模块，统一支持标题栏返回、系统返回键和返回手势。

### 安装与升级

- 版本：1.3.0（versionCode 5），应用ID：`com.legion.viewer`。
- 支持Android 8.0及以上，仅ARM64；沿用原正式签名，可覆盖安装旧正式版。
- 保留现有目录索引及播放、阅读进度，不需要数据库迁移。
- 全部功能已通过用户实机验收。
- 正式构建通过83项单元测试和Release Lint（0错误）；APK签名与旧版一致，SHA-256校验通过。
- 下载附件：`Viewer-Android-arm64-v1.3.0.apk`；校验文件：`SHA256SUMS.txt`。

## 1.2.0

本版本跳过1.1，包含以下优化：

- **沉浸阅读**：TXT、Markdown和漫画支持单击正文任意位置显隐上下工具栏及系统栏；内容就绪后无操作3秒自动隐藏，滚动和拖动不会误触显隐。内容可延伸至顶部状态栏与屏幕缺口区域。
- **历史位置提示**：视频、音乐恢复成功后显示“已定位至上次播放”；文本和漫画显示“已定位至上次阅读”，3秒后消失，新内容不提示。
- **返回顶部与撤销**：文本和漫画新增悬浮返回顶部按钮，所在行透明；返回后3秒内可点击“撤销”恢复原位置。
- **漫画目录分类**：以漫画文件夹的父目录分类，支持展开/折叠；图片文件夹继续作为一本漫画，点击封面直接阅读。
- **持久目录索引**：四类内容首次导入后保存列表，重启应用或手机后直接载入；手动刷新或重新选择目录才重新扫描。刷新失败保留原列表。
- **目录点击反馈**：四类目录展开/折叠的点击背景使用与目录框一致的圆角。

### 安装与升级

- 版本：1.2.0（versionCode 4），应用ID：`com.legion.viewer`。
- 支持Android 8.0及以上，仅ARM64；沿用原正式签名，可覆盖安装原正式版。
- 从旧版本升级后，每个已配置分类首次进入时需补建一次目录索引，之后无需重复扫描。
- 已通过用户实机验收、48项单元测试、Release Lint及APK签名校验。
- 下载附件：`Viewer-Android-arm64-v1.2.0.apk`；校验文件：`SHA256SUMS.txt`。

### 后续计划

“搜索”功能将在1.3版本推出。
